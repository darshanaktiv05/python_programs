# my_tuple = ('php', 'swift', 'odoo', 'python', 'c++', 'c')
#
# updated_list = map(lambda x: x.upper(), my_tuple)
#
# print(list(updated_list))
#
# print(tuple(map(lambda x: x.lower(), my_tuple)))
# print(set(map(lambda x: x.lower(), my_tuple)))

my_dict = {2, 3, 4, 5, 6, 7, 8, 9}
final = map(lambda x: x, my_dict)

print(list(final))
